# Backend API origin. Used at build/dev time via Vite (`import.meta.env.VITE_API_BASE_URL`).
VITE_API_BASE_URL=http://localhost:8000

# Mock Google OAuth + onboarding in localStorage. Set to false when backend auth is ready.
VITE_USE_MOCK_AUTH=true
